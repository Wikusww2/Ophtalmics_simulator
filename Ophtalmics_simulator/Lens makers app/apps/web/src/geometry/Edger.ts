// Placeholder for edging operations with axis lock.
export type EdgingOptions = { axisLock: boolean }

export function performEdgingPlaceholder(_opts: EdgingOptions) {
  // In a full implementation, this would CSG-intersect the lens solid with the frame profile
  // while preserving rotation (axis) if axisLock is true.
}

